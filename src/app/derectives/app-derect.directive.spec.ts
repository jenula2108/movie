import { AppDerectDirective } from './app-derect.directive';

describe('AppDerectDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDerectDirective();
    expect(directive).toBeTruthy();
  });
});
